---
title: Explicit stacks (terragrunt.stack.hcl)
status: stub
tags:
  - terragrunt
---

# Explicit stacks (terragrunt.stack.hcl)

!!! note "Stub page"
    Define a reusable infrastructure pattern (e.g. app + db + queue) once, instantiate per environment.

## Planned content

- `terragrunt.stack.hcl` syntax: `unit`, `stack`, `values`, dependencies
- When to use stacks vs raw units
- Catalog repo pattern (units + stacks in a separate repo)
- `no_dot_terragrunt_stack` for in-place migration
